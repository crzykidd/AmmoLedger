# Standards implemented

This project implements the following [standards](https://gitea.crzynet.com/crzynet/homelab-configs/src/branch/main/standards)
from the crzynet `homelab-configs` repo. Each row pins the **version** that this
project has actually wired up. Bumping a pin is a deliberate act (re-implement against
the new revision in the same commit that updates the pin); standards do not auto-upgrade
adopters.

| Standard | Version | Adopted | Notes |
|---|---|---|---|
| [code-checkin-and-pr](https://gitea.crzynet.com/crzynet/homelab-configs/src/branch/main/standards/code-checkin-and-pr/README.md) | 1.2.0 | 2026-07-02 | Upgraded 1.1.0 → 1.2.0 on 2026-07-02. v1.2.0 adds two required checks (test suite + SAST/code-scanning) to the prior five. Now **fully v1.2.0-conformant**: backend lint / YAML validation / migration-to-head / `docker compose config` / **backend test suite** (`test-backend` job) in [.github/workflows/ci.yml](.github/workflows/ci.yml); **SAST** via **GitHub CodeQL default setup** (already enabled in repo Security settings since 2026-05-18, scanning Python + JS/TS on PR + weekly — no committed workflow needed); PR-only image-build verification (build, no push) in [.github/workflows/docker-publish.yml](.github/workflows/docker-publish.yml). Frontend has no test suite (no vitest) so it is exempt from the test-suite check per the standard. Fixed the one stale test assertion (`test_zip_restore_rejects_path_traversal`) so the suite runs green. Publishing matrix and registry retention (keep 30 `sha-*` / 15 semver; `latest`/`dev`/bare-major protected) unchanged in `docker-publish.yml`. The CLAUDE-snippet (operational rules) is unchanged 1.1.0→1.2.0. **Follow-up (manual, needs repo admin):** add `test-backend` to `main`'s required status checks in branch protection once it's run on a PR — see `docs/decisions.md`. |
| [release-prep-and-cut](https://gitea.crzynet.com/crzynet/homelab-configs/src/branch/main/standards/release-prep-and-cut/README.md) | 1.1.0 | 2026-07-02 | Upgraded 1.0.0 → 1.1.0 on 2026-07-02. v1.1.0's only delta is **summarize-on-archive**: when a new minor/major fires the archive trigger, the active `CHANGELOG.md` now keeps a condensed `## [<version>] (summary)` block (major-item bullets + `[full notes]` deep link) in place of each archived minor instead of moving it out wholesale, and every closed minor still in the active file is archived in one pass. Re-implemented in `.claude/commands/release-prep.md` (Step 3) and the `CLAUDE.md` "Changelog Process" + "Release Process → Archive trigger" descriptions. The per-session CLAUDE-snippet (operational rules) is unchanged 1.0.0→1.1.0. Already-archived 0.3.x/0.2.x/0.1.x left as-is (going-forward only; the next minor bump summarizes 0.4.x) — see `docs/decisions.md`. `/release-prep` + `/release-cut` remain the standard's templates tailored to AmmoLedger (`backend/version.py`). |
| [handoff-prompt-workflow](https://gitea.crzynet.com/crzynet/homelab-configs/src/branch/main/standards/handoff-prompt-workflow/README.md) | 2.0.0 | 2026-06-07 | Upgraded 1.5.0 → 2.0.0 on 2026-06-07. v2.0.0 now ships a `CLAUDE-snippet.md` and carries a per-session default (push any >1–2-file edit into a `prompts/` file and execute it via a **spawned subagent**), so the soft pointer is no longer sufficient: snippet pasted verbatim into `CLAUDE.md` ("Handoff prompts (operational rules)"), replacing the old soft pointer. `prompts/TEMPLATE.md` re-synced to v2.0.0 (spawned-agent vs manual-fallback commit split). Existing prompts and `docs/decisions.md` left untouched. |
| [repo-sandbox-permissions](https://gitea.crzynet.com/crzynet/homelab-configs/src/branch/main/standards/repo-sandbox-permissions/README.md) | — (de-adopted) | de-adopted 2026-07-02 | **De-adopted 2026-07-02** — the working copy now lives on a dedicated dev host with a lowered risk profile, so OS-level command confinement is no longer worth its friction. Was wired at v1.0.0 (2026-05-29, local-only scope). Back-out (config-driven standard, so minimal): `sandbox` block removed from `.claude/settings.local.json` (that file is gitignored + untracked, so not a committed change; the `Read(...)` allows remaining there are unrelated manual entries, not the standard's `Read(**)/Edit(**)/Write(**)` globs); `.claude/settings.json` never carried the block; no `CLAUDE-snippet.md` existed to remove; `.gitignore` entry for `.claude/settings.local.json` left in place (general hygiene, not sandbox-specific). Host runtime deps (`bwrap`/`socat`) teardown is out of scope for this app repo. Original adoption record preserved in `docs/decisions.md` (2026-05-29 entry). |
| [vexp-context-engine](https://gitea.crzynet.com/crzynet/homelab-configs/src/branch/main/standards/vexp-context-engine/README.md) | — (de-adopted / sunset) | de-adopted 2026-06-01 | **De-adopted 2026-06-01** per the v3.0.0 removal guide (vexp sunset homelab-wide). Repo wiring stripped: `git rm .claude/hooks/vexp-guard.sh`; `PreToolUse` guard + `mcp__vexp__*` allows removed from `.claude/settings.json`; "Context search (operational rules)" CLAUDE-snippet removed from `CLAUDE.md`; vexp ignore block + `!.claude/hooks/` un-ignore dropped from `.gitignore`; `.vexpignore`, `.vexp/.gitignore`, `vexp.toml`, and the auto-generated `.github/copilot-instructions.md` untracked and `.vexp/` removed on disk. Host install teardown is out of scope for this app repo — handled by the `ansible` `devworkstation` role's opt-in `--tags vexp_teardown` task. Was wired at v2.0.0 (2026-05-30). |
